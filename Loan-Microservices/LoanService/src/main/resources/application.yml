eureka:
  client:
    register-with-eureka: true
    fetchRegistry: true  #fetch the list of services from Eureka
    serviceUrl:
      defaultZone: http://localhost:8761/eureka
  instance:
    hostname: localhost
    
server:
  port: 8091
  
spring:
  application:
    name: LOAN-SERVICE